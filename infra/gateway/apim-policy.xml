<policies>
    <inbound>
        <base />
        <!-- Authentication: APIM subscription key (Ocp-Apim-Subscription-Key header).
             Subscription key validation is handled natively by APIM —
             no additional policy required. -->
    </inbound>
    <backend>
        <base />
    </backend>
    <outbound>
        <base />
    </outbound>
    <on-error>
        <base />
    </on-error>
</policies>
